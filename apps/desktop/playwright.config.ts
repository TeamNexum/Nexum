import { defineConfig, devices } from "@playwright/test";

// E2E runs against the plain-browser preview (no Tauri backend), so it exercises
// the UI shell, tab navigation, the browser guard banner, and the cloud sign-in
// flow (with the network mocked). Playwright starts the Vite dev server itself.
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  use: {
    baseURL: "http://127.0.0.1:1420",
    trace: "on-first-retry",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run dev",
    url: "http://127.0.0.1:1420",
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
